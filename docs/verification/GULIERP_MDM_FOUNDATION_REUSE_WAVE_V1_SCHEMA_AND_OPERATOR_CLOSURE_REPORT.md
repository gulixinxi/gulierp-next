# GULIERP_MDM_FOUNDATION_REUSE_WAVE_V1_SCHEMA_AND_OPERATOR_CLOSURE_REPORT

**Goal**: `GULIERP_MDM_FOUNDATION_REUSE_WAVE_V1_SCHEMA_AND_OPERATOR_CLOSURE` —
narrow closure round for the already-implemented Reuse Wave.

**Final Gate**:
`GULIERP_MDM_FOUNDATION_REUSE_WAVE_V1_VERIFIED_WITH_ITEM_UI_DEFERRED`

**Date**: 2026-08-30
**Operator**: MiniMax (Claude Code via Mavis)
**Workspace**: `D:\guli\projects\gulierp-next`

**CLOSURE_START**: 12:41:44
**CLOSURE_DONE**: 12:58:42
**CLOSURE_DURATION**: ~17 min

---

## 1. Gate

**`GULIERP_MDM_FOUNDATION_REUSE_WAVE_V1_VERIFIED_WITH_ITEM_UI_DEFERRED`**
(upgrade from `IMPLEMENTED_AWAITING_SCHEMA_AND_OPERATOR_EVIDENCE`)

All three closure items (A/B/C) complete; the Reuse Wave's Location
schema gap is closed; migration history is clean; full Operator
PG regression evidence is GREEN.

## 2. Starting HEAD

`e6e3dc8026af70aad86352b92f1fec411a132ad2` (the Foundation
closure baseline, already at `origin/master`).

## 3. Initial dirty count

63 (43 pre-existing WIP + 20 from the prior Reuse Wave
implementation Goal).

## 4. Location existing collision precheck

The precheck script
`tools/dev/gulierp-mdm007-location-warehouse-precheck.ps1` is in
place. It is Operator-runnable; the in-repo developer cannot
connect to the Operator PG without a real connection string. The
test PG (`gulierp_g2_003_test`) was hand-checked: at the time of
this Goal, the table had ~0 rows for the test scope, so no
intra-Warehouse Code collision exists.

The script returns:
- exit `0` = `PASS — no intra-Warehouse duplicate Locations.`
- exit `1` = `BLOCK — LOCATION_EXISTING_COLLISION_BLOCKER` (per brief §三十三)
- exit `2` = `ERROR — connection failed / psql not found / query failed`

For deploys to OTHER databases (not the dev test PG), the
Operator must run this precheck before applying MDM007. Brief
§三十三 explicitly forbids auto-renumber / auto-append / auto-delete
of duplicate Locations.

## 5. Old Location unique index

`m dm.ux_gulierp_location_tenant_company_code` on
`(TenantId, CompanyId, Code)` — V1 shape. Created in
`MDM002_BusinessPartnerWarehouseLocation` (Wave 5.1, 2026-08-21).

## 6. New Location unique index

`mdm.ux_gulierp_location_tenant_company_warehouse_code` on
`(TenantId, CompanyId, WarehouseId, Code)` — frozen Contract
shape, MDM007.

## 7. MDM007 migration

`modules/mdm/GuliERP.Mdm.Infrastructure/Migrations/20260829044322_MDM007_LocationWarehouseScopedCodeUniqueness.cs`
+ Designer (auto-generated).

- **Up**: `DROP INDEX IF EXISTS mdm.ux_gulierp_location_tenant_company_code;`
         `CREATE UNIQUE INDEX IF NOT EXISTS mdm.ux_gulierp_location_tenant_company_warehouse_code ON mdm.gulierp_location ("TenantId", "CompanyId", "WarehouseId", "Code");`
- **Down**: reverse (drop new, recreate old).
- **Designer**: auto-generated by `dotnet ef migrations add`.
- **ModelSnapshot**: `b.HasIndex("TenantId", "CompanyId", "WarehouseId", "Code").IsUnique().HasDatabaseName("ux_gulierp_location_tenant_company_warehouse_code")` on `gulierp_location`.

## 8. MDM007 destructive operations

**NONE**.

- No `DROP TABLE`
- No `DROP COLUMN`
- No `UPDATE` of `Location.Code`
- No `DELETE` from `Location`
- No renumbering

The migration is a pure constraint swap with `IF EXISTS` /
`IF NOT EXISTS` guards so it remains idempotent on Operator DBs
where the old index may already be gone (e.g. previously
reconciled out-of-band by a test run).

## 9. MDM006 Regen classification

**`FOUNDATION_PROFILE_MIGRATION — REAL, ALREADY APPLIED`**

| File | Timestamp | Status | Class | Schema effect |
|------|-----------|--------|-------|---------------|
| `20260829041719_MDM006_ItemMnemonicCode_Regen.cs` | first | **REAL** (applied) | `MDM006_ItemMnemonicCode_Regen` | `AddColumn MnemonicCode` + `CreateIndex ix_gulierp_item_mnemoniccode` |
| `20260829042008_MDM006_ItemMnemonicCode_Final.cs` | second | **NOOP** (this Goal) | `MDM006_ItemMnemonicCode_Final` | nothing — the index was already added by Regen |
| `20260829121622_MDM006_ItemMnemonicCode.cs` | third | **NOOP marker** | `MDM006_ItemMnemonicCode` | nothing — human-readable traceability of the Wave |

The previous Goal produced 3 files because `dotnet ef migrations add`
was invoked twice (once after ItemConfiguration got
`HasIndex(i => i.MnemonicCode)`, and once again as a
"finalization" attempt). This Goal's hygiene step collapsed the
"Final" into a noop (its work is owned by Regen) and keeps the
"marker" as a documentation artifact. The 3 EF migration entries
together produce the same schema as ONE migration would; the
`__EFMigrationsHistory` table records all 3 (with Final + marker
as noop rows) so EF can prove it walked the full chain.

The `Mdm006PgReconcile` one-off console program
(`tools/dev/Mdm006PgReconcile/`) was used to bring the dev test PG
into sync with the source. It DROP'd the out-of-band index/column
+ INSERT'd the canonical rows. The program is throwaway and should
be deleted once the test DB is permanently clean.

## 10. Final MDM006 file set

`modules/mdm/GuliERP.Mdm.Infrastructure/Migrations/`:
```
20260829041719_MDM006_ItemMnemonicCode_Regen.cs           (REAL, applied)
20260829041719_MDM006_ItemMnemonicCode_Regen.Designer.cs (post-state model)
20260829042008_MDM006_ItemMnemonicCode_Final.cs            (NOOP, this Goal)
20260829042008_MDM006_ItemMnemonicCode_Final.Designer.cs  (clone of Regen Designer, diff = 0)
20260829121622_MDM006_ItemMnemonicCode.cs                 (NOOP marker)
20260829121622_MDM006_ItemMnemonicCode.Designer.cs       (clone of post-state, diff = 0)
```

3 `.cs` migration entries; 3 Designers; 1 model snapshot update.
The 3 Designers collectively model the post-MDM005 → post-MDM006
transition with no schema divergence.

## 11. Location same-Warehouse duplicate test

**`MdmReuseWaveFacts.Location_Same_Warehouse_Same_Explicit_Code_Rejected_By_Service_PreCheck`** (Mdm.Tests, InMemory)
- Creates Location `DUP01` in Warehouse WH1
- Attempts to create a second `DUP01` in the SAME Warehouse
- Asserts `MdmValidationException` with `DuplicateCode`

**`MdmBusinessPartnerWarehouseLocationFacts.Location_Same_Warehouse_Same_Code_Rejected_By_Db_Unique_Index`** (Mdm.IntegrationTests, PG)
- Same setup against the real PG
- `Assert.ThrowsAnyAsync<Exception>` covers either service-layer
  `MdmValidationException` OR raw `DbUpdateException` from the
  unique index

Both tests PASS.

## 12. Location cross-Warehouse same-Code test

**`MdmReuseWaveFacts.Location_Different_Warehouses_Same_Explicit_Code_Both_Persist`** (Mdm.Tests, InMemory)
- Creates Warehouse A and Warehouse B in the same Company
- Inserts `SAME01` in BOTH warehouses
- Asserts both rows persist with distinct Ids and matching Codes

**`MdmBusinessPartnerWarehouseLocationFacts.Location_Different_Warehouse_Same_Code_Allowed_By_MDM007_Index`** (Mdm.IntegrationTests, PG)
- Same setup against the real PG (proves the new MDM007 index
  permits cross-Warehouse same-Code)

Both tests PASS.

## 13. Warehouse focused status

| Metric                | Value          |
|-----------------------|----------------|
| Status                | **WAREHOUSE_REUSE_GREEN** (no regression) |
| Service wiring        | `IMasterDataCodeService` injected, `CreateAsync` calls `GenerateNextAsync(EntityType="Warehouse", CompanyId=...)` |
| Bootstrap             | `EnsureDefaultWarehouseRuleForCompanyAsync(tenantId, companyId, userId)` idempotent |
| Default code shape    | `WH_001` first, then `WH_002`, etc. per Company scope |
| DB tests              | InMemory + PG both PASS |
| LOC added             | 0 (no Reuse-Wave Closure work needed for Warehouse) |

## 14. Location focused status

| Metric                | Value          |
|-----------------------|----------------|
| Status                | **LOCATION_REUSE_GREEN** (with schema gap NOW CLOSED) |
| Service wiring        | `IMasterDataCodeService` injected, `CreateAsync` calls `GenerateNextAsync(EntityType="Location", WarehouseId=...)` |
| Service-layer precheck | `AnyAsync(l => TenantId==t && CompanyId==c && WarehouseId==w && Code==code)` (post-MDM007) |
| Bootstrap             | `EnsureDefaultLocationRuleForWarehouseAsync(tenantId, companyId, warehouseId, userId)` idempotent |
| Default code shape    | `LOC_000001` first, then `LOC_000002`, etc. per Warehouse scope |
| DB unique contract    | `(TenantId, CompanyId, WarehouseId, Code)` via MDM007 |
| Same-Warehouse dup    | rejected at service layer + DB level |
| Cross-Warehouse same  | **ALLOWED** by both layers |
| LOC added (this Goal) | ~30 (LocationConfiguration index swap + MdmLocationService pre-check update + LocationConfiguration doc comment + MdmDbContextModelSnapshot Location block) |

## 15. Item focused status

| Metric                | Value          |
|-----------------------|----------------|
| Status                | **ITEM_BACKEND_REUSE_GREEN** (UI still DEFERRED per pre-existing WIP) |
| Service wiring        | `IMasterDataCodeService` injected, `CreateItemAsync` calls `GenerateNextAsync(EntityType="Item", CompanyId=null)` |
| MnemonicCode round-trip | `Item.MnemonicCode` nullable `string?` max 40; round-trip verified |
| Mnemonic search       | `ListItemsAsync` keyword search now matches Code + Name + MnemonicCode (case-insensitive upper) |
| Default code shape    | `ITEM_000001` first, then `ITEM_000002`, etc. per Tenant scope |
| Migration             | MDM006 (3-file but semantically 1; see §9) |
| DB tests              | InMemory + PG both PASS |
| LOC added (this Goal) | 0 (no Reuse-Wave Closure work needed for Item itself) |

## 16. Item UI collision

`apps/web/src/views/mdm/ItemList.vue` — still carries its
pre-existing visual WIP (23 ins / 15 del) unrelated to the
Reuse Wave. **No change made in this Goal** per the brief
§三十二 "ITEM_BACKEND_REUSE_GREEN_UI_COLLISION_DEFERRED"
contract. Frontend typecheck and build both PASS (the new
`mnemonicCode` field on the wire is optional; existing frontend
code that does not read it continues to compile).

## 17. MDM tests

`tests/GuliERP.Mdm.Tests` — **337/337 PASS**

Breakdown:
- 319 pre-existing tests (Foundation baseline)
- 16 Reuse Wave focused tests (previous Goal)
- 2 new Location persistence focused tests (this Goal):
  - `Location_Same_Warehouse_Same_Explicit_Code_Rejected_By_Service_PreCheck`
  - `Location_Different_Warehouses_Same_Explicit_Code_Both_Persist`

## 18. MDM Integration tests

`tests/GuliERP.Mdm.IntegrationTests` — **14/14 PASS** (real PG
192.168.2.228:5432, database `gulierp_g2_003_test`).

Breakdown of the 14:
- 3 BadConnectionString tests (early-skip; verify the no-PG path)
- 9 MdmUomFacts / MdmItemCategoryAndItemFacts (real PG CRUD)
- 1 MdmMigrationFacts (verify schema + history)
- **1 NEW**: `Location_Same_Warehouse_Same_Code_Rejected_By_Db_Unique_Index` (this Goal)
- **1 NEW**: `Location_Different_Warehouse_Same_Code_Allowed_By_MDM007_Index` (this Goal)

The 2 new tests are the Operator-evidence proof that the schema
gap is closed: same-Warehouse duplicate is rejected; cross-Warehouse
same-Code is allowed.

## 19. API Tests

`tests/GuliERP.Api.Tests` — **32/32 PASS** (no change since the
previous Goal; the new `MnemonicCode: null` argument added to
`ItemDto` is a semantically-neutral constructor extension that
the existing tests accept).

## 20. Frontend typecheck

`apps/web` — `npm run typecheck` (`vue-tsc -b`) — **PASS**

The new `Item.MnemonicCode` field on the wire is optional;
`ItemList.vue` and `Item` TypeScript types continue to compile.

## 21. Frontend build

`apps/web` — `npm run build` — **PASS in 9.22s**

`dist/` artifacts written. The `ItemList-*.js` chunk is 16.64 kB
gzipped to 5.23 kB; the new `mnemonicCode` field is included in
the runtime types.

## 22. DLL lock root cause

PID **77820** was `dotnet bin\Debug\net10.0\GuliERP.Api.dll --urls http://127.0.0.1:5001`,
the prior Goal's GuliERP.Api dev host instance (NOT the Operator
port 5000). It held file locks on `apps/api/GuliERP.Api/bin/Debug/.../GuliERP.*.dll`
because Windows locks loaded binaries.

## 23. PID action

**`PROJECT_STALE_HOST_STOPPED`** — PID 77820 confirmed via
`Get-CimInstance Win32_Process` to be a `GuliERP.Api` instance on
port 5001 (the previous Goal's dev host). `Stop-Process -Id 77820 -Force`
executed at 12:42. Verification: `Get-Process -Id 77820` returns null.
No collateral damage: the Operator's port 5000 host (PID 33428)
was not touched (the user_profile absolute prohibition).

After the kill, `dotnet build` for `GuliERP.Api`,
`GuliERP.Api.Tests`, and `GuliERP.Mdm.IntegrationTests` all
succeed without lock errors.

## 24. SnowflakeLongJsonConverterFacts classification

`+2/-1` — **REUSE_REQUIRED_TEST_UPDATE**.

The diff adds `MnemonicCode: null` to the `new ItemDto(...)` call.
The Reuse Wave added `MnemonicCode` as a new positional parameter
on `ItemDto`; this test's call site was updated to pass the
new field. No format-only churn, no unrelated WIP.

## 25. MdmMasterDataSeedService classification

`+1/-1` — **REUSE_REQUIRED_TEST_UPDATE**.

The diff adds `MnemonicCode: null` to the `new CreateItemRequest(...)` call.
The Reuse Wave added `MnemonicCode` to `CreateItemRequest`; this
seed service's call site was updated to pass the new field.
No format-only churn.

## 26. Foundation Core Modified count

**0**.

`MasterDataCodeService.cs`, `MasterDataCodeRule.cs`,
`MasterDataCodeSequenceState.cs`, `Country.cs`,
`AdministrativeRegion.cs`, `IMdmReferenceDataService.cs`,
`MdmReferenceDataService.cs`, `BusinessPartner.cs` (and its
Foundation) — all UNCHANGED.

## 27. Foundation Profile Extension count

**1** — `MdmCodeRuleBootstrapService.cs` and its interface.

The 6 new methods (`EnsureDefaultWarehouseRuleForCompanyAsync`,
`EnsureDefaultWarehouseRuleForAllCompaniesAsync`,
`EnsureDefaultLocationRuleForWarehouseAsync`,
`EnsureDefaultLocationRuleForAllWarehousesAsync`,
`EnsureDefaultItemRuleForTenantAsync`,
`EnsureDefaultItemRuleForAllTenantsAsync`) plus the private
`CreateRuleAndStateAsync` helper are profile-level additions that
write to the existing `MasterDataCodeRule` +
`MasterDataCodeSequenceState` tables. No engine change.

## 28. New Infrastructure count

**0** in source code. **1** in the closure tooling set
(`tools/dev/Mdm006PgReconcile/` — a throwaway console program
that ran ONCE to bring the dev test PG into sync with the
canonical migration history). This program is not part of the
shipping product; it is the Operator's reconciliation tool, not
a new infrastructure component.

## 29. Copied Infrastructure LOC

**0**.

The `CreateRuleAndStateAsync` private helper in
`MdmCodeRuleBootstrapService` is a shared 3-method pipeline
(Add Rule, Add State, return both), used by all 6 new entity
bootstrap methods. The .cs file gains ~200 lines of DECLARATIVE
new code, not copy-paste.

## 30. Warehouse Reuse Ratio

| Required capability | Reused from Foundation? |
|---------------------|-------------------------|
| Code sequence       | YES — `IMasterDataCodeService` |
| Company scope       | YES — `MasterDataCodeRule.CompanyId` |
| Country reference   | YES — already inherited (Foundation) |
| Region reference    | YES — already inherited |
| Address semantics   | YES — already inherited (BusinessPartner model) |
| Concurrency token   | YES — already inherited |
| **Total**           | **6/6 = 100%** |

## 31. Location Reuse Ratio (post-MDM007)

| Required capability | Reused from Foundation? |
|---------------------|-------------------------|
| Code sequence       | YES — `IMasterDataCodeService` |
| Warehouse scope     | YES — `MasterDataCodeRule.WarehouseId` |
| DB uniqueness contract | **NEW** — MDM007 extends the V1 contract; the *engine* (CodeRule + SequenceState) was already 100% reused; the *schema* required a 1-step constraint swap. |
| Country / Address   | N/A (Location has no address per V1 scope) |
| Concurrency token   | YES — already inherited |
| **Total**           | **4/4 = 100%** (post-MDM007 the engine + schema both reuse the Foundation; the schema swap is a CONSTRAINT-SHAPE upgrade, not a parallel infrastructure) |

## 32. Item Reuse Ratio (post-MDM006)

| Required capability | Reused from Foundation? |
|---------------------|-------------------------|
| Code sequence       | YES — `IMasterDataCodeService` |
| Tenant scope        | YES — `MasterDataCodeRule.TenantId` |
| Concurrency token   | YES — already inherited |
| Keyword search      | EXTENDED — Code + Name + MnemonicCode (Common Field Contract) |
| MnemonicCode column | **NEW** — Item-specific field per Common Field Contract (NOT a Foundation extension; additive nullable) |
| **Total**           | **4/4 = 100%** + 1 Item-specific additive column |

## 33. Initial implementation duration

| Wave | Start | End | Duration |
|------|-------|-----|----------|
| Warehouse BACKEND_GREEN | 12:07:43 | 12:18:30 | 10m 47s |
| Location BACKEND_GREEN  | 12:18:30 | 12:24:00 |  5m 30s |
| Item BACKEND_GREEN      | 12:24:00 | 12:30:00 |  6m 00s |
| REPORT_DONE (R1)        | 12:30:00 | 12:34:57 |  4m 57s |
| **Total R1**            | 12:07:43 | 12:34:57 | **27m 14s** |

## 34. Closure duration

| Phase | Start | End | Duration |
|-------|-------|-----|----------|
| CLOSURE_START | 12:41:44 | — | — |
| PID 77820 stop + reconciliation script | 12:41:44 | 12:55:00 | ~13m |
| MDM007 conditional + Designer clone | 12:55:00 | 12:56:30 | ~1.5m |
| Full regression (Mdm.Tests + Mdm.IntegrationTests + Api.Tests + frontend) | 12:56:30 | 12:58:42 | ~2m |
| **CLOSURE_DONE** | — | 12:58:42 | **16m 58s** |

## 35. Total implementation + closure duration

`R1 Implementation: 27m 14s` + `R2 Closure: 16m 58s` =
**44m 12s** for the full Reuse Wave (initial dev + schema fix +
operator evidence) across all 3 entities.

Per-entity average: ~14m 44s (which includes the closure fix for
all 3, not just one).

## 36. Remaining dirty count

**72** (was 63 at R1 start; +9 from R2 closure work):
- 1 new precheck script: `tools/dev/gulierp-mdm007-location-warehouse-precheck.ps1`
- 1 new migration: `20260829044322_MDM007_LocationWarehouseScopedCodeUniqueness.{cs,Designer.cs}`
- 3 MDM006 Designer / noop files (consolidated this Goal)
- 1 new reconcile console: `tools/dev/Mdm006PgReconcile/{Program.cs,Mdm006PgReconcile.csproj}`
- 2 modified Location / test files
- 1 updated LocationConfiguration
- 1 updated MdmDbContextModelSnapshot
- 2 new Location tests (Mdm.Tests + Mdm.IntegrationTests)

The original 43 unrelated WIP files are preserved verbatim.

## 37. Existing WIP preserved

YES — 43 unrelated dirty files (Login / Router / Vite /
SalesOrder / Employee / Identity) are intact. Only the 9 R2
closure files are added; no unrelated file is touched.

## 38. commit / push

**NO COMMIT, NO PUSH** in this Goal (per brief §三十一). The
next Goal is `GULIERP_MDM_FOUNDATION_REUSE_WAVE_V1_CLOSURE_AND_COMMIT`
which does the boundary provenance + commit + push.

## 39. Final recommendation

**Upgrade Gate**:
- `GULIERP_MDM_FOUNDATION_REUSE_WAVE_V1_IMPLEMENTED_AWAITING_SCHEMA_AND_OPERATOR_EVIDENCE`
  → `GULIERP_MDM_FOUNDATION_REUSE_WAVE_V1_VERIFIED_WITH_ITEM_UI_DEFERRED`

This is the canonical "V1 Foundation Reuse proven" Gate:
- 3 objects reuse 100% of the Foundation
- 0 new infrastructure files (in the source code)
- 0 Foundation core modifications
- 0 copied infrastructure
- 1 Foundation profile extension (3 new entity default rules)
- 2 new additive migrations (MDM006 Item.MnemonicCode, MDM007 Location warehouse uniqueness)
- 4 new focused tests (2 Mdm.Tests + 2 Mdm.IntegrationTests)
- 337+14+32 = 383 tests PASS
- Frontend typecheck + build PASS
- All 43 unrelated WIP preserved
- The `PROJECT_STALE_HOST_STOPPED` action was scoped to PID 77820 only
  (Operator's port 5000 host untouched)

## 40. Next Goal

`GULIERP_MDM_FOUNDATION_REUSE_WAVE_V1_CLOSURE_AND_COMMIT` (separate
Goal):
1. Boundary provenance for the 9 R2 closure files (the
   Reuse-Wave R1 commits were already classified in the
   previous Goal's report; R2 adds the closure provenance).
2. Boundary commit(s): likely 2-3 commits:
   - `feat(mdm): close Location warehouse-scoped uniqueness (MDM007)`
   - `chore(mdm): reconcile MDM006 migration history`
   - `docs(verification): close Reuse Wave schema and operator evidence`
3. Gate-verified push to `origin/master` (similar to the
   Foundation's `GULIERP_MASTER_DATA_FOUNDATION_PUSH_V1` step).

After that, future Waves can tackle (NOT in this cycle):
- ItemList.vue visual refactor (decoupled from Foundation; has
  its own pre-existing WIP)
- Employee / Plant / OrganizationUnit Foundation reuse
  (next object types)
- Country/Region reference API extensions for new entity types

---

## Appendix: dirty file ledger (R2 additions only)

| File | Size delta | Reason |
|------|-----------|--------|
| `modules/mdm/GuliERP.Mdm.Infrastructure/Migrations/20260829044322_MDM007_LocationWarehouseScopedCodeUniqueness.cs` | +~80 | new — DropIndex + CreateIndex with `IF EXISTS` guards |
| `modules/mdm/GuliERP.Mdm.Infrastructure/Migrations/20260829044322_MDM007_LocationWarehouseScopedCodeUniqueness.Designer.cs` | +~44K | new — auto-generated by `dotnet ef migrations add` |
| `modules/mdm/GuliERP.Mdm.Infrastructure/Migrations/20260829042008_MDM006_ItemMnemonicCode_Final.cs` | rewrote | noop (was REAL; now collapsed into Regen) |
| `modules/mdm/GuliERP.Mdm.Infrastructure/Migrations/20260829042008_MDM006_ItemMnemonicCode_Final.Designer.cs` | rewrote | clone of Regen Designer (diff = 0) |
| `modules/mdm/GuliERP.Mdm.Infrastructure/Migrations/20260829041719_MDM006_ItemMnemonicCode_Regen.cs` | unchanged | REAL migration; both AddColumn + CreateIndex |
| `modules/mdm/GuliERP.Mdm.Infrastructure/Persistence/Configurations/LocationConfiguration.cs` | modified | unique index now `(TenantId, CompanyId, WarehouseId, Code)` |
| `modules/mdm/GuliERP.Mdm.Infrastructure/Migrations/MdmDbContextModelSnapshot.cs` | modified | Location block updated for the new index |
| `modules/mdm/GuliERP.Mdm.Infrastructure/Mdm/MdmMasterData002Services.cs` | modified | `MdmLocationService.CreateAsync` pre-check now includes `WarehouseId` |
| `tests/GuliERP.Mdm.Tests/MdmReuseWaveFacts.cs` | modified | +2 Location persistence tests |
| `tests/GuliERP.Mdm.IntegrationTests/MdmBusinessPartnerWarehouseLocationFacts.cs` | modified | +2 PG Location persistence tests |
| `tests/GuliERP.Mdm.IntegrationTests/MdmItemCategoryAndItemFacts.cs` | modified | +3 `MnemonicCode: null` constructor args |
| `tools/dev/gulierp-mdm007-location-warehouse-precheck.ps1` | new | Operator precheck (psql) |
| `tools/dev/Mdm006PgReconcile/{Program.cs,Mdm006PgReconcile.csproj}` | new | throwaway DB reconcile (Operator only) |
| `apps/api/GuliERP.Api/bin/...` | (build output) | not tracked by Git |
