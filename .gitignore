bin/
obj/
node_modules/
dist/
.vite/
.vs/
.idea/
*.user
*.suo
.env
.env.*
!.env.example

# MDM-000D-R1: discovery intermediate output (large normalized JSON dumps).
# Re-creatable via tools/discovery/mdm-000d/extract_dev_metadata.py.
# Tracked by tools/discovery/mdm-000d/_canonical/normalized-manifest.json (SHA-256).
tools/discovery/mdm-000d/_normalized/


# Local runtime / browser automation artifacts.
.runtime-browser-profile/
.stack-logs/
.spec-workflow/
.mcp.json

# Frontend incremental build cache.
apps/web/tsconfig.tsbuildinfo

# Local generated artifacts and test evidence output.
artifacts/
tests/**/TestResults/
tests/_evidence_trx/

# Raw governance / evidence extraction dumps (may contain internal IPs,
# local paths, and bulky raw database extracts; intentionally not committed
# — see the G3 git closeout 2026-08-26 for the audit trail).
docs/governance/extracted/

# Temporary quarantine / diagnostic scratch files.
tools/.quarantine/
modules/mdm/GuliERP.Mdm.Infrastructure/tools/.quarantine/

# Discovery transient Python/cache/log output.
tools/discovery/**/__pycache__/
tools/discovery/**/*.pyc
tools/discovery/**/_*.log
